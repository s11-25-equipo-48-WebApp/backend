export enum Status {
  PENDIENTE = 'pendiente',
  APROBADO = 'aprobado',
  RECHAZADO = 'rechazado',
}

export enum MediaType {
  IMAGE = 'image',
  VIDEO = 'video',
  NONE = 'none',
}

export enum Role {
  VISITOR = 'visitor',
  EDITOR = 'editor',
  ADMIN = 'admin',
  SUPERADMIN = 'superadmin',
}

export enum EventType {
  VIEW = 'view',
  CLICK = 'click',
  SHARE = 'share',
}
