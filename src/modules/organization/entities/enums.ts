export enum Role {
  VISITOR = 'visitor',
  EDITOR = 'editor',
  ADMIN = 'admin',
  SUPERADMIN = 'superadmin',
}

export enum Status {
  PENDIENTE = 'pendiente',
  APROBADO = 'aprobado',
  RECHAZADO = 'rechazado',
}